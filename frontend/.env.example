VITE_API_URL=http://localhost:8000

# From your Supabase project: Settings -> API. The anon key is safe to expose
# client-side (Row Level Security enforces access) — never put the
# service_role key here or anywhere in frontend code.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
